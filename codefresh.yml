version: '1.0'
steps:
  test:
    title: Run tests
    image: python
    commands:
      - pip install --upgrade yamllint
      - find /codefresh/volume/gkube -maxdepth 1 -type f -exec yamllint {} \;
